import { LogoutButton } from "./buttons";

export default function DashboardPage() {
  return (
    <main>
      <header>
        <LogoutButton />
      </header>
      <div>this is dashboard</div>
    </main>
  );
}
